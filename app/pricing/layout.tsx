// This layout file intentionally passes through to avoid metadata conflicts.
// Metadata is defined in page.tsx.
export default function PricingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
